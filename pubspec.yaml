name: learn_animate
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  flame: ^1.21.0
  flutter:
    sdk: flutter
  get: ^4.6.6
  simple_game:
    path: ../simple_game

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
